apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository

metadata:
  name: healthsecure-manifests
  namespace: flux-system

spec:
  interval: 1m

  url: oci://${account_id}.dkr.ecr.${region}.amazonaws.com/healthsecure-manifests

  ref:
    tag: staging